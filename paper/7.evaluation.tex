% \section{Empirical Validation}
% \label{sec:empirical_validation}

% We empirically validate the proposed identifiability-aware source
% apportionment framework in a controlled city-scale pollution setting. The
% goal of the empirical study is not merely to show that source
% contributions can be fitted to sparse sensor observations, but to test
% whether attribution accuracy is predicted by the identifiability geometry
% of the projected lagged source-response matrix
% \[
% \widetilde H = P_Q^\perp H_{1:T}^{\mathrm{lag}}.
% \]
% Accordingly, the experiments evaluate how rank, conditioning, effective
% rank, source visibility, pairwise coherence, background correction, wind
% diversity, and sensor placement affect source-level attribution.

% \subsection{Pollution Simulator and Sensing Setup}
% \label{subsec:pollution_setup}

% We simulate pollutant transport on a normalized
% \([0,1]\times[0,1]\) domain corresponding to the New Delhi region using
% the two-dimensional advection--diffusion--source equation
% \begin{align}
% \partial_t u
% +
% V_x(t)\,\partial_x u
% +
% V_y(t)\,\partial_y u
% =
% k\,\Delta u
% +
% S(x,y,t).
% \label{eq:empirical_advection_diffusion}
% \end{align}
% The state is discretized on a \(40\times 40\) grid. Advection is
% approximated with first-order upwind differences, diffusion with a
% five-point Laplacian stencil, and time integration uses a two-stage Heun
% (RK2) update.

% The physical regime is advection dominated. In the simulator, the base
% wind is set to \((V_x,V_y)=(1.12,0.984)\), and the diffusivity is fixed
% to \(k=3\times 10^{-4}\), with additional diurnal modulation and AR(1)
% variability in the wind field. The rollout horizon is \(T=20\), with
% \(N_t=10{,}000\) time steps and
% \[
% \Delta t = T/(N_t-1) \approx 2\times 10^{-3}.
% \]

% The sensor layout is obtained by mapping real regulatory monitor
% locations onto the simulation grid. Although the source location file
% contains 33 monitoring sites, one pair collapses to the same cell after
% discretization, yielding \(m=32\) unique sensor locations on the
% \(40\times 40\) grid. Thus, the sensor density is
% \[
% m/q = 32/1600 = 0.02,
% \]
% where \(q=40\times 40=1600\) is the number of source cells on the fine
% grid. This sparse sensing regime is the setting in which fine-grained
% source attribution is expected to be limited by the geometry of the
% sensor network and the wind-conditioned source fingerprints.

% The initial condition is not learned in these experiments. Instead, it is
% constructed from government monitoring data
% \citep{cpcb,cpcb_portal} through Universal Kriging. This allows the
% empirical study to focus on source attribution under sparse observations,
% rather than ambiguity induced by an unknown initial condition.

% \subsection{Source Inventories and Ground-Truth Contributions}
% \label{subsec:source_inventories_ground_truth}

% We use inventory-based source groups rather than arbitrary unknown source
% fields. The source-inventory matrix is
% \[
% S
% =
% \begin{bmatrix}
% \mathbf s_1 &
% \mathbf s_2 &
% \cdots &
% \mathbf s_K
% \end{bmatrix}
% \in
% \mathbb R^{q\times K},
% \]
% where each column represents a spatial source pattern. The available
% proxy inventories include brick kiln and industrial emissions
% \citep{GUTTIKUNDA2013101}, population-density
% \citep{ColumbiaUniversity2018}, and traffic-intensity
% \citep{google_maps} maps, cropped from \(80\times 80\) to the
% \(40\times 40\) simulation window and normalized.

% \textcolor{red}{
% Experiment task: Construct a set of \(K\) source groups from these
% inventories. At minimum, use traffic, industrial/brick-kiln, and
% population-related emissions as separate groups. Optionally split each
% inventory into spatial subgroups, such as north/south or near/far
% regions, to create controlled ambiguity between source fingerprints.
% }

% Ground-truth observations are generated by choosing a nonnegative source
% activity vector
% \[
% \boldsymbol\theta^\star \in \mathbb R_+^K
% \]
% and simulating the source field
% \[
% S(x,y,t)
% =
% \sum_{k=1}^K
% \theta_k^\star s_k(x,y).
% \]
% The resulting concentration field is sampled at the fixed sensor
% locations to produce clean sensor observations \(Y_{\mathrm{clean}}\).

% \textcolor{red}{
% Experiment task: Generate multiple ground-truth activity vectors
% \(\boldsymbol\theta^\star\), including sparse, dense, balanced, and
% near-collinear source mixtures. These controlled settings will allow us
% to evaluate when the estimated activity vector
% \(\widehat{\boldsymbol\theta}\) is accurate and when attribution becomes
% unstable.
% }

% \subsection{Noise Model}
% \label{subsec:noise_model}

% Observation noise is injected directly into the simulated sensor
% trajectories. The clean sensor matrix
% \(Y_{\mathrm{clean}}\in\mathbb R^{m\times T}\) is perturbed as
% \begin{equation}
% Y_{\mathrm{noisy}}
% =
% Y_{\mathrm{clean}}
% +
% \varepsilon,
% \qquad
% \varepsilon_{ij}
% \sim
% \mathcal N(0,\sigma^2),
% \label{eq:empirical_noise_model}
% \end{equation}
% with
% \begin{equation}
% \sigma
% =
% 0.1
% \max_{i,t}
% |Y_{\mathrm{clean}}(i,t)|.
% \label{eq:empirical_relative_noise}
% \end{equation}
% Thus, the noise scale is relative to the magnitude of the observed
% signal, rather than being an absolute constant. This provides a
% controlled counterpart to the empirically noisy sensing regime observed
% in sparse air-pollution monitoring systems
% \citep{bhardwaj2022learning}.

% \textcolor{red}{
% Experiment task: Repeat the attribution experiments across multiple
% noise levels, for example \(1\%\), \(5\%\), \(10\%\), and \(20\%\) of the
% maximum clean sensor signal. For each noise level, compute
% \(\tau_\sigma\), the effective rank of \(\widetilde H\), and the source
% activity estimation error.
% }

% \subsection{Experiment 1: Identifiability Predicts Attribution Accuracy}
% \label{subsec:identifiability_predicts_accuracy}

% The first experiment tests the central claim that attribution accuracy is
% controlled by the projected lagged response matrix
% \[
% \widetilde H
% =
% P_Q^\perp H_{1:T}^{\mathrm{lag}}.
% \]
% For each simulated setting, we construct \(H_{1:T}^{\mathrm{lag}}\) using
% the wind-conditioned transport operator, apply the background projection,
% and estimate source contributions by solving
% \[
% \widehat{\boldsymbol\theta}
% =
% \arg\min_{\boldsymbol\theta\ge 0}
% \left\|
% \widetilde{\mathbf Y}
% -
% \widetilde H\boldsymbol\theta
% \right\|_2^2
% +
% \lambda R(\boldsymbol\theta).
% \]

% \textcolor{red}{
% Experiment task: For each trial, compute the attribution error
% \[
% \|\widehat{\boldsymbol\theta}-\boldsymbol\theta^\star\|_2,
% \]
% and compare it against identifiability diagnostics:
% \(\sigma_{\min}(\widetilde H)\), condition number
% \(\kappa(\widetilde H)\), effective rank
% \(r_{\mathrm{eff}}(\tau_\sigma)\), and source visibility \(v_k\).
% The expected result is that attribution error increases when
% \(\sigma_{\min}(\widetilde H)\) decreases, when
% \(\kappa(\widetilde H)\) increases, or when effective rank drops below
% \(K\).
% }

% \textcolor{red}{
% Experiment task: Report scatter plots showing attribution error versus
% \(\sigma_{\min}(\widetilde H)\), attribution error versus
% \(\kappa(\widetilde H)\), and per-source error versus visibility
% \(v_k\). These plots should demonstrate that the proposed diagnostics
% predict when source apportionment is reliable.
% }

% \subsection{Experiment 2: Pairwise Ambiguity and Source Merging}
% \label{subsec:pairwise_ambiguity_merging}

% The second experiment evaluates whether pairwise source-fingerprint
% coherence identifies source groups that should not be interpreted
% separately. For each pair of source groups \(i,j\), we compute
% \[
% \rho_{ij}
% =
% \frac{
% |\widetilde{\mathbf h}_i^\top \widetilde{\mathbf h}_j|
% }{
% \|\widetilde{\mathbf h}_i\|_2
% \|\widetilde{\mathbf h}_j\|_2
% }.
% \]
% High coherence indicates that two source groups produce nearly
% proportional sensor-time fingerprints after background correction.

% \textcolor{red}{
% Experiment task: Construct controlled source pairs with increasing
% coherence. This can be done by placing source groups along similar
% upwind/downwind paths, by splitting one inventory into nearby spatial
% subregions, or by creating near/far source pairs whose transported
% sensor fingerprints are similar up to scale.
% }

% \textcolor{red}{
% Experiment task: Compare separate-source estimation against merged-source
% estimation. For high-coherence pairs, evaluate whether the separate
% estimates \(\widehat\theta_i\) and \(\widehat\theta_j\) are unstable
% while the combined estimate
% \[
% \widehat\theta_i+\widehat\theta_j
% \]
% is more accurate. This directly validates the source-merge rule.
% }

% \textcolor{red}{
% Experiment task: Report a table containing pairwise coherence
% \(\rho_{ij}\), separate-source error, merged-source error, and the
% resulting merge recommendation.
% }

% \subsection{Experiment 3: Background Correction and Source Absorption}
% \label{subsec:background_confounding_experiment}

% The third experiment studies how background correction affects
% identifiability. We model background and temporal variation using a
% low-dimensional basis \(Q\), such as time-of-day spline terms,
% day-level intercepts, regional trends, and sensor offsets. The relevant
% response matrix is then
% \[
% \widetilde H
% =
% P_Q^\perp H_{1:T}^{\mathrm{lag}},
% \]
% not the raw response matrix \(H_{1:T}^{\mathrm{lag}}\).

% \textcolor{red}{
% Experiment task: Generate observations with known source contributions
% and an added background component \(Q\boldsymbol\gamma^\star\). Then
% compare attribution with no background correction, moderate background
% correction, and overly flexible background correction.
% }

% \textcolor{red}{
% Experiment task: For each source group \(k\), compute the background
% absorption fraction
% \[
% a_k
% =
% \frac{
% \|P_Q \mathbf h_k^{\mathrm{lag}}\|_2
% }{
% \|\mathbf h_k^{\mathrm{lag}}\|_2
% }.
% \]
% Evaluate whether sources with large \(a_k\) have larger attribution
% error after background projection.
% }

% \textcolor{red}{
% Experiment task: Show that background correction can improve robustness
% by removing broad temporal confounding, but can also reduce
% identifiability if the background basis is too flexible and absorbs
% source-driven variation.
% }

% \subsection{Experiment 4: Wind Diversity and Sensor Geometry}
% \label{subsec:wind_sensor_geometry_experiment}

% The fourth experiment evaluates how wind diversity and sensor placement
% affect the rank and conditioning of \(\widetilde H\). Since source
% fingerprints are wind-conditioned, a single dominant wind direction may
% make some sources difficult to separate, while diverse wind directions
% can expose sources from multiple transport angles.

% \textcolor{red}{
% Experiment task: Run the simulator under different wind regimes:
% constant wind, diurnally varying wind, AR(1)-perturbed wind, and
% multi-directional wind episodes. For each regime, compute the singular
% values, effective rank, visibility scores, and pairwise coherence of
% \(\widetilde H\).
% }

% \textcolor{red}{
% Experiment task: Vary the number and placement of sensors. Compare the
% real regulatory sensor layout against random layouts, downwind-focused
% layouts, and layouts chosen to maximize
% \(\sigma_{\min}(\widetilde H)\) or minimize pairwise coherence.
% }

% \textcolor{red}{
% Experiment task: Report how attribution error changes as a function of
% sensor density, sensor placement, and wind diversity. The expected result
% is that denser and more strategically placed sensors improve
% conditioning and reduce source ambiguity.
% }

% \subsection{Experiment 5: Wind and Transport-Operator Uncertainty}
% \label{subsec:wind_uncertainty_experiment}

% The final experiment evaluates sensitivity to errors in the constructed
% response matrix. In practice,
% \(H_{1:T}^{\mathrm{lag}}\) depends on estimated wind fields, dispersion
% parameters, and transport approximations. These errors perturb
% \(\widetilde H\) and therefore affect source attribution.

% \textcolor{red}{
% Experiment task: Perturb the wind field by adding controlled errors in
% wind speed and direction. For each perturbation level, reconstruct
% \(\widehat{\widetilde H}\), estimate
% \(\widehat{\boldsymbol\theta}\), and measure attribution error.
% }

% \textcolor{red}{
% Experiment task: Compare attribution error against the response-matrix
% perturbation magnitude
% \[
% \|\widehat{\widetilde H}-\widetilde H^\star\|_2.
% \]
% This tests the perturbation bound from the identifiability theory.
% }

% \textcolor{red}{
% Experiment task: If wind ensembles are available, construct an ensemble
% of response matrices \(\widetilde H^{(b)}\), refit source contributions
% for each ensemble member, and report empirical uncertainty intervals for
% \(\widehat{\boldsymbol\theta}\).
% }

% \subsection{Evaluation Metrics}
% \label{subsec:evaluation_metrics}

% Across all experiments, we evaluate both source attribution accuracy and
% identifiability diagnostics. The source-level metrics are:
% \begin{itemize}[itemsep=0pt,topsep=0pt,leftmargin=*]
%     \item activity-vector error:
%     \(\|\widehat{\boldsymbol\theta}
%     -
%     \boldsymbol\theta^\star\|_2\);
%     \item relative activity error:
%     \[
%     \frac{
%     \|\widehat{\boldsymbol\theta}
%     -
%     \boldsymbol\theta^\star\|_2
%     }{
%     \|\boldsymbol\theta^\star\|_2
%     };
%     \]
%     \item per-source absolute error:
%     \(|\widehat\theta_k-\theta_k^\star|\);
%     \item merged-group error for ambiguous source groups;
%     \item fitted sensor trajectory error:
%     \[
%     \|\widetilde{\mathbf Y}
%     -
%     \widetilde H\widehat{\boldsymbol\theta}\|_2.
%     \]
% \end{itemize}

% The identifiability metrics are:
% \begin{itemize}[itemsep=0pt,topsep=0pt,leftmargin=*]
%     \item rank of \(\widetilde H\);
%     \item smallest singular value \(\sigma_{\min}(\widetilde H)\);
%     \item condition number \(\kappa(\widetilde H)\);
%     \item effective rank \(r_{\mathrm{eff}}(\tau_\sigma)\);
%     \item source visibility \(v_k\);
%     \item normalized visibility \(\bar v_k\);
%     \item pairwise coherence \(\rho_{ij}\);
%     \item ray distance \(d^{\mathrm{ray}}_{ij}\);
%     \item background absorption fraction \(a_k\);
%     \item merge recommendations.
% \end{itemize}

% \textcolor{red}{
% Experiment task: The final empirical claim should be that source
% apportionment error is not arbitrary: it is systematically predicted by
% the identifiability structure of
% \(\widetilde H=P_Q^\perp H_{1:T}^{\mathrm{lag}}\). The experiments
% should show that well-conditioned source fingerprints are accurately
% apportioned, weakly visible sources are unstable, high-coherence sources
% should be merged, and background or wind uncertainty reduces the
% identifiable source resolution.
% }







\section{Empirical Validation}
\label{sec:empirical_validation}

The empirical study evaluates whether source attribution error is predicted by
the identifiability structure of
\(\widetilde H_\Phi=P_Q^\perp H_{\Phi}^{\mathrm{lag}}\).  The experiments use a
controlled city-scale advection--diffusion simulator so that ground-truth source
activities, wind regimes, noise levels, background components, and sensor
layouts can be varied independently.

\subsection{Pollution Simulator and Sensor Layout}
\label{subsec:pollution_simulator}

We simulate pollutant transport on a normalized \([0,1]\times[0,1]\) domain
corresponding to the New Delhi region using
\begin{equation}
\partial_t u + V_x(t)\partial_x u + V_y(t)\partial_y u
= k\Delta u + S(x,y,t).
\label{eq:simulator_pde}
\end{equation}
The state is discretized on a \(40\times40\) grid.  Advection is approximated
with first-order upwind differences, diffusion with a five-point Laplacian
stencil, and time integration with a two-stage Heun update.  The base wind is
\((V_x,V_y)=(1.12,0.984)\), the diffusivity is \(k=3\times10^{-4}\), and the
wind includes diurnal modulation and AR(1) variability.  The rollout horizon is
\(T=20\) with \(N_t=10{,}000\) integration steps, giving
\(\Delta t\approx2\times10^{-3}\).

Sensor locations are obtained by mapping regulatory monitoring sites from the
New Delhi region to the simulation grid.  The raw location file contains 33
sites, but one pair collapses to the same grid cell, yielding \(m=32\) unique
sensor locations.  With \(q=40\times40=1600\) source cells, the sensor density is
\(m/q=0.02\).  Initial conditions are constructed from government monitoring
data using Universal Kriging rather than learned jointly, so that the
experiments focus on source attribution rather than initial-condition recovery
\citep{cpcb,cpcb_portal}.

\subsection{Inventories and Ground Truth}
\label{subsec:inventories_ground_truth}

The source-inventory matrix
\(S=[\mathbf s_1\;\cdots\;\mathbf s_K]\in\mathbb R^{q\times K}\) is built from
proxy maps, including brick-kiln and industrial emissions
\citep{GUTTIKUNDA2013101}, population density \citep{ColumbiaUniversity2018},
and traffic intensity derived from map data \citep{google_maps}.  The maps are
cropped from \(80\times80\) to the \(40\times40\) simulation window and
normalized.  At minimum, traffic, industrial or brick-kiln activity, and
population-related emissions are treated as separate source groups.  To create
controlled ambiguity, inventories can also be split into spatial subgroups such
as north/south, near/far, or upwind/downwind regions.

Ground-truth observations are generated by choosing nonnegative temporal-basis
coefficients \(C^\star\in\mathbb R_+^{K\times B}\), reconstructing
\(\theta_k^\star(t)=\sum_b c_{kb}^\star\phi_b(t)\), and simulating
\begin{equation}
S(x,y,t)=\sum_{k=1}^K\theta_k^\star(t)s_k(x,y).
\label{eq:ground_truth_source_field}
\end{equation}
We evaluate sparse, dense, balanced, and near-collinear coefficient mixtures,
including the constant-activity case \(B=1\), to test both identifiable and
ambiguous regimes.

\subsection{Noise and Background Settings}
\label{subsec:noise_background_settings}

Clean sensor trajectories \(Y_{\mathrm{clean}}\in\mathbb R^{m\times T}\) are
perturbed by Gaussian observation noise,
\begin{equation}
Y_{\mathrm{noisy}} = Y_{\mathrm{clean}}+\epsilon,
\qquad
\epsilon_{ij}\sim\mathcal N(0,\sigma^2),
\label{eq:noise_model}
\end{equation}
with \(\sigma\) set to 1\%, 5\%, 10\%, or 20\% of the maximum clean sensor signal.
Background components are generated from low-dimensional temporal and sensor
bases, including smooth time-of-day variation, day-level intercepts, regional
trends, and sensor offsets.  This allows comparison among no correction,
moderate correction, and overly flexible correction.

\subsection{H1: Conditioning Predicts Attribution Error}
\label{subsec:h1_conditioning}

The first validation tests the central prediction that coefficient and
reconstructed-activity errors are controlled by the conditioning of
\(\widetilde H_\Phi\).  For each trial, we construct
\(H_{\Phi}^{\mathrm{lag}}\), project by \(P_Q^\perp\), fit
\(\widehat{\mathbf c}\), reconstruct \(\widehat\theta_k(t)\), and compute
\begin{equation}
\|\widehat{\mathbf c}-\mathbf c^\star\|_2,
\qquad
\|\widehat\Theta-\Theta^\star\|_F.
\label{eq:activity_error}
\end{equation}
We compare these errors with \(\sigma_J(\widetilde H_\Phi)\),
\(\kappa(\widetilde H_\Phi)\), \(r_{\mathrm{eff}}(\tau_\sigma)\), and
coefficient visibility \(v_j\).  The expected pattern is increasing attribution
error as \(\sigma_J\) decreases, \(\kappa\) increases, or effective rank falls
below \(J\).  Activity errors should be largest for sources containing weak
source--basis fingerprints.

\subsection{H2: Coherent Sources Should Be Merged}
\label{subsec:h2_merging}

The second validation tests whether pairwise coherence identifies source groups
that should not be interpreted separately.  We construct source pairs with
increasing \(\rho_{ij}\) by placing groups along similar transport paths,
splitting a single inventory into nearby subregions, or creating near/far pairs
whose fingerprints differ mainly by scale.  For each pair, we compare separate
estimation error with merged-group activity-trajectory error,
\begin{equation}
\left\|
(\widehat{\boldsymbol\theta}_{i,1:T}
+\widehat{\boldsymbol\theta}_{j,1:T})
-(\boldsymbol\theta_{i,1:T}^\star
+\boldsymbol\theta_{j,1:T}^\star)
\right\|_2.
\label{eq:merged_error}
\end{equation}
High-coherence pairs should exhibit unstable separate estimates but more stable
combined estimates, validating the merge rule.

\subsection{H3: Background Correction Can Help or Hurt}
\label{subsec:h3_background}

The third validation studies how the background basis changes identifiability.
For each coefficient fingerprint \(j=(k,b)\), we compute
\begin{equation}
a_j=\frac{\|P_Q\mathbf h_j^{\mathrm{lag}}\|_2}
{\|\mathbf h_j^{\mathrm{lag}}\|_2}.
\label{eq:evaluation_absorption}
\end{equation}
Moderate background correction should improve robustness when it removes broad
temporal confounding.  Overly flexible correction should reduce projected
visibility and increase attribution error for coefficients with large
absorption fractions.

\subsection{H4: Wind Diversity and Sensor Geometry Change Resolution}
\label{subsec:h4_wind_sensor_geometry}

The fourth validation varies the wind regime and sensor layout.  Wind settings
include constant wind, diurnally varying wind, AR(1)-perturbed wind, and
multi-directional wind episodes.  Sensor settings include the regulatory layout,
random layouts, downwind-focused layouts, and layouts selected to increase
\(\sigma_J(\widetilde H_\Phi)\) or reduce maximum eligible coherence.  For each
setting, we report singular values, effective rank, visibility, coherence, ray
distance, and source activity error.  Diverse wind and strategically placed
sensors should improve the identifiable temporal source resolution more than
spatial coverage alone.

\subsection{H5: Response-Matrix Error Amplifies Attribution Error}
\label{subsec:h5_operator_uncertainty}

The final validation perturbs the constructed response matrix through wind and
transport errors.  We add controlled errors to wind speed and direction,
construct \(\widehat{\widetilde H}_\Phi\), refit source activities, and compare
attribution error with
\begin{equation}
\|\widehat{\widetilde H}_\Phi-\widetilde H_\Phi^\star\|_2.
\label{eq:evaluation_operator_error}
\end{equation}
When wind ensembles are available, we construct \(\widetilde H_\Phi^{(b)}\),
refit \(\widehat{\mathbf c}^{(b)}\), reconstruct activities, and report
empirical uncertainty intervals.
The perturbation bound predicts that response-matrix error is most damaging
when \(\widehat{\widetilde H}_\Phi\) is ill-conditioned.

\subsection{Metrics}
\label{subsec:evaluation_metrics}

Across experiments, we report both attribution accuracy and identifiability
structure.  Accuracy metrics include absolute and relative coefficient error,
source activity-trajectory error, per-source error, merged-group error, and
fitted sensor-trajectory error
\(\|\widetilde Y-\widetilde H_\Phi\widehat{\mathbf c}\|_2\).
Identifiability metrics include numerical rank, \(\sigma_J\), condition number,
effective rank, coefficient visibility, coherence, ray distance, background
absorption, and merge recommendations.  Coherence and ray distance are N/A for
pairs involving \(v_j\le\tau_v\); those entries are excluded from extrema while
weak coefficients remain explicitly flagged.  Cross-source summaries retain
the eligible coefficient pair attaining maximum coherence and minimum ray
distance.

The empirical claim is that source apportionment error is systematic rather
than arbitrary: well-conditioned and visible fingerprints should be accurately
apportioned, weakly visible coefficients should produce unstable activity
components, high-coherence sources should be reported after merging, and wind
or background uncertainty should reduce the identifiable temporal source
resolution.
