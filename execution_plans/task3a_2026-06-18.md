# Task 3A Plan: Clean Slate Active Tree

## Summary

Make the tracked repository active tree IASA-only. Keep optional local reference copies under ignored `archive/`, but ensure clean checkouts neither track nor depend on archived heat, SWE, free-field pollution, ablation, SimGrad, notebook, or MPRNN/STAMP workflows.

## Key Changes

- Move or remove legacy notebooks, checkpoints, heat/SWE modules, free-field pollution generators/evaluators/tuners, ablation simulators, prior training code, and obsolete runtime helpers from active tracked paths.
- Preserve useful local copies under ignored `archive/`; treat that directory as non-contract and optional.
- Remove stale imports, package exports, README commands, tests, and roadmap references that require archived modules.
- Remove remaining aggregate/free-field compatibility placeholders from `PolGrid` and active pollution APIs.
- Keep only named pollution inventories, New Delhi weather/imputation code, IASA runtime scripts, tests, and shared utilities required by the active workflow.
- Add `scripts/run_iasa_sanity.py` as a concrete minimal inventory/weather/activity sanity command.

## Verification

- No tracked notebooks, checkpoint directories, archive files, or retired legacy modules.
- No active imports from `archive/`.
- No active references to retired source, heat/SWE, SimGrad, or prior APIs.
- Every remaining tracked Python module imports in the supported container.
- Source/weather tests, runtime smoke, and minimal IASA sanity runner pass.

## Assumptions

- Backward compatibility with removed workflows is not required.
- `archive/` may be absent from a clean checkout.
- The active target is IASA pollution/source apportionment only.

