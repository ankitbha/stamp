# Task 11A — Update paper with results + reconcile drifts (execution plan, 2026-07-22)

## Objective
Edit `paper/` to (1) replace every "Results pending" placeholder in `8.evaluation.tex`
with real tables/numbers from `evaluation/iasa_pol/reports/`, and (2) reconcile the 8
documented implementation drifts so paper and code agree. `paper/` is now editable.
No tests/gates (per current guidance); rebuild `main.pdf` if a LaTeX toolchain exists,
else leave the build to the user and verify `.tex` by careful review.

## Inputs
- `evaluation/iasa_pol/reports/report.md` + per-table CSVs + `report.json` (Task 11 output).
- Committed runs provenance (40x40, T=72 controlled; observed weeks 1-4 T=168; git SHAs).

## Edits
### A. Results tables (paper/8.evaluation.tex) — replace each "Results pending"
- Exp 1-10: insert a compact LaTeX table per subsection using the columns we ACTUALLY
  computed (from reports), + 1-2 sentence reading of the result.
- Observed: 4 subsections as weeks 1-4 (Tier 0) — identifiability/report groups;
  proxy apportionment (fraction of fitted sensor signal, per week); sensor fit &
  residual diagnostics (uncalibrated); wind-imputation validation (kernel imputer,
  FieldFormer not adopted). Per-monitor footprints summarized (full table too large;
  cite the CSV artifact).

### B. Drift reconciliations
1. Observed = windowed weeks 1-4 (Tier 0, no cross-window aggregation); note the
   week-to-week attribution swing → §Study Modes + observed results (7/8.evaluation).
2. Record length: controlled T=72, observed weekly windows (not full record).
3. Wind: kernel coordinate-query imputer adopted; FieldFormer trained-not-adopted
   (RMSE FF 1.12 / kernel 1.55 / city-mean 1.06) → §Masked Wind Preparation (4.method),
   §Wind Preparation (7.evaluation).
4. Kriging → Gaussian-kernel surrogate (Pusa averaged) via open-boundary puff Green's
   function → §Forward Models (7.evaluation).
5. Controlled sources: real maps for E2/E6, compact synthetic for others → §Study Modes.
6. Traffic = one road map + slot bases + F0 admissibility; reconcile the "traffic_06
   unsupported" sentence with the single-road-map design → §Inventories + apportionment.
7. Platform-based E8 (non-empty Q; in-span absorbed, residual-visible detected).
8. Additions: briefly note Gate S7 + reporting layer in the appendix (optional).

### C. Column-drift from upstream gaps (from Task 11 audit)
Where the paper promised columns the runner never computed (exp01 activity error/
coverage; exp02 ray distance/component label; exp04 effective-rank/condition/min ray
distance/groups/activity error; exp05 operator ids/condition/coverage; exp06 explicit
versions/perturbation cols/grouped contribs; exp07 convergence ratio/effective rank/
condition status; exp09 per-source/basis rows): adjust the table DESCRIPTION to the
computed columns (do NOT fabricate; do NOT re-run experiments).

## Steps
1. plan (this) → 2. read report values + paper sections → 3. edit 8.evaluation.tex
(results) → 4. edit 7/4/9 (.tex drifts) → 5. strikethrough roadmap 11A heading →
6. rebuild main.pdf if toolchain present → 7. commit → 8. subagent audit (paper vs
code/reports, no fabricated numbers, no contradictions) → 9. fix + commit.
