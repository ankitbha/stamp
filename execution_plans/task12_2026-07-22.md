# Task 12 — Documentation and cleanup (execution plan, 2026-07-22)

## Objective
Make the IASA workflow discoverable and accurate; ensure docs match the shipped
code and reconciled paper (no stale FieldFormer-adopted / ImputeFormer / legacy
claims), and mark archived legacy files as out-of-contract. No tests/gates (docs
task, per current guidance).

## Files
- `README.md` — rewrite into the IASA entry point: runtime env, quickstart minimal
  run, gates, controlled sweep, observed weekly windows, summarize + reporting,
  paper build; environment (container, torch/CUDA, device/dtype defaults, SLURM);
  interpretation caveats; legacy/archive note; pandas-numpy-vs-torch statement;
  no SciPy as required solver.
- `docs/iasa_workflow.md` (new) — the 12-step recommended workflow, full command
  reference, provenance/artifact schema, and the paper defaults
  (`tau_L=1e-3`, `alpha=0.05`, 1000 adequacy refits, wind quantiles 0.05/0.5/0.95).
- `ROADMAP_STAMP_NEW.md` — strikethrough the Task 12 heading.

## Facts to document accurately (verify against code)
- Runtime: Singularity .sif + ext3 overlay, `source /ext3/env.sh`; SLURM account
  `torch_pr_633_general`, partition `l40s_public`, `--gres=gpu:1`.
- Wind: adopted = kernel coordinate-query imputer (`model/iasa/wind.py`); FieldFormer
  trained but NOT adopted (held-out RMSE FF 1.12 / kernel 1.55 / city-mean 1.06).
- Device/dtype: inverse/diagnostics/fit float64, response float32
  (`model/iasa/backend.py`); explicit device (cpu/cuda).
- Gates: `scripts/run_iasa_sanity.py --gate {task3a,response,...,experiments,
  reporting,all}` and `--gate all --strict-all`.
- Runners: `experiments/iasa_pol/run_experiment.py --config ... --device cuda`;
  summarizers `summarize_results.py`, `summarize_weeks.py`; reporting
  `evaluation/eval_pol_iasa.py`.
- Paper build: `pdflatex` with `TEXINPUTS=.:./icml2026//:` (algorithm.sty vendored).
- Caveats: identifiability certificates conditional on declared inventory/transport/
  basis/lag/background/mask/noise; adequacy rejection => inadequacy not cause,
  non-rejection !=> completeness; conservative merge groups not the finest partition;
  inventory scenarios are robustness rows, not CIs.
- Legacy: `archive/` is gitignored and out-of-contract; active code must not depend
  on it. Remove any heat/SWE/SimGrad/free-field/old-calibration workflow text
  (README already clean; verify).

## Steps
1. plan (this) → 2. verify device/dtype + gate/runner facts in code → 3. rewrite
README.md → 4. write docs/iasa_workflow.md → 5. strikethrough roadmap Task 12 →
6. commit → 7. subagent audit (commands/defaults/paths correct; no stale claims;
matches code) → 8. fix + commit.
